extends layout

block content
  h1= title

  ul
    each plant in plant_list
      li
        a(href=plant.url) #{plant.name}
        |   (#{plant.greenhouse.name})


    else
      li There are no plants.
